# TicTacToe

Console-based tic-tac-toe game implemented with board, pieces, rules, and controller classes.

## Structure

- `src/Main.java`: Entry point.
- `src/model/`: Core game logic and strategy classes.

## Build And Run

```bash
javac -d out $(find src -name "*.java")
java -cp out Main
```